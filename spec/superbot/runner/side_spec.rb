RSpec.describe Superbot::Runner::Side do
  it "has a version number" do
    expect(Superbot::Runner::Side::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
